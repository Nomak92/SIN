SIN (Server Into Netbox)
========================

SIN is a tool to import data from a server into Netbox. Currently it supports Cisco servers only.


Usage:
------

```
main.py --csv import.csv
```

